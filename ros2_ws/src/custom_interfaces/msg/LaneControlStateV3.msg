# Backward-compatible geometry extension. V2 remains unchanged so old bags
# and existing tools keep their original ROS type contract.
custom_interfaces/LaneControlStateV2 control
float32 preview_signed_curvature_hint
